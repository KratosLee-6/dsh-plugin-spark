# Third-party notices / 第三方声明

Spark's code, synthetic Skill examples, SVG mark and CSS artwork are original to this repository and distributed under MIT. No artwork or component code was copied from Apple, Refero, React Bits, Aceternity UI or other design galleries. System fonts are referenced by name, not redistributed.

Spark 的代码、合成技能示例、SVG 标志和 CSS 图形由本项目原创，以 MIT 分发。未复制 Apple、Refero、React Bits、Aceternity UI 等网站的图形或组件源码；只引用系统字体名称，不分发字体文件。

Runtime peers / 运行时对等依赖:

- `@deepseek-ai/cordis` 4.0.2 — MIT.
- `@deepseek-ai/dsh-tools` 0.1.5-rc.2 — MIT.
- Node.js supplies `node:sqlite`, HTTP and cryptography. Node.js and SQLite retain their upstream notices. / Node.js 提供 SQLite、HTTP 和加密模块，保留上游声明。

Development dependencies, including DeepSeek Harness CLI, TypeScript, Vitest and Playwright, retain the license files supplied with their npm packages. The lockfile records exact resolved versions. Spark's package does not bundle these dependencies into its own code; package managers install their distributions separately.

开发依赖（含 DSH CLI、TypeScript、Vitest、Playwright）保留其 npm 包附带许可证，精确版本记录于 lockfile。Spark 不将它们合并进自身源码包，由包管理器分别安装。
